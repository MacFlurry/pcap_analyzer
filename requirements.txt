# Production dependencies for PCAP Analyzer
# These match the install_requires in setup.py with upper bounds to prevent breaking changes

scapy>=2.5.0,<3.0
dpkt>=1.9.8,<2.0  # Fast packet parsing (3-5x faster than Scapy)
paramiko>=3.4.0,<4.0
pyyaml>=6.0,<7.0
jinja2>=3.1.2,<4.0
rich>=13.7.0,<14.0
click>=8.1.7,<9.0
numpy>=1.24.0,<2.0  # Critical: NumPy 2.0 has breaking changes
