# Architecture Design - PCAP Analyzer Web Interface

**Date:** 2025-12-12
**Version:** 1.0
**Complément:** DESIGN_SYSTEM.md, DESIGN_MOCKUPS.md, DESIGN_CODE_SNIPPETS.md

Ce document présente l'architecture visuelle et structurelle du design.

---

## Vue Globale - Flow Utilisateur

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER JOURNEY FLOW                           │
└─────────────────────────────────────────────────────────────────┘

START
  │
  ▼
┌──────────────────┐
│ LANDING PAGE     │  📍 URL: /
│ (upload.html)    │
│                  │
│ - Hero section   │
│ - Drag & drop    │
│ - Recent history │
└────────┬─────────┘
         │
         │ [User selects PCAP file]
         │
         ▼
    File Validation
         │
         ├──[VALID]──────────────────────┐
         │                               │
         │ [User clicks "Analyze"]       │
         │                               │
         ▼                               │
    API: POST /api/upload               │
         │                               │
         │ Response: {task_id}           │
         │                               │
         ▼                               │
┌──────────────────┐                     │
│ PROGRESS PAGE    │  📍 URL: /progress/{task_id}
│ (progress.html)  │                     │
│                  │                     │
│ - SSE stream     │◄────────────────────┘
│ - Phase 1/2      │
│ - Memory gauge   │
│ - Live log       │
└────────┬─────────┘
         │
         │ [Analysis completes]
         │
         ▼
    Auto-redirect (3s)
         │
         ▼
┌──────────────────┐
│ REPORT PAGE      │  📍 URL: /report/{task_id}
│ (report.html)    │
│                  │
│ - Iframe embed   │
│ - Download btns  │
│ - Share link     │
└────────┬─────────┘
         │
         │ [User can:]
         │
         ├──[Download HTML]──► File download
         │
         ├──[Download JSON]──► File download
         │
         ├──[New Analysis]───► Back to Landing
         │
         └──[View History]───┐
                             │
                             ▼
                    ┌──────────────────┐
                    │ HISTORY PAGE     │  📍 URL: /history
                    │ (history.html)   │
                    │                  │
                    │ - Search/filter  │
                    │ - Analysis cards │
                    │ - Statistics     │
                    └────────┬─────────┘
                             │
                             │ [User clicks analysis]
                             │
                             └──► REPORT PAGE
```

---

## Architecture des Pages

### Page Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                        BASE TEMPLATE                            │
│                        (base.html)                              │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ HEADER (Sticky)                                           │  │
│  │ ┌─────────────┐                            ┌────────────┐ │  │
│  │ │ 📊 PCAP     │  [Upload] [History]        │ Theme 🌙  │ │  │
│  │ │   Analyzer  │                            │ Toggle     │ │  │
│  │ └─────────────┘                            └────────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ MAIN CONTENT (Dynamic, varies by page)                    │  │
│  │                                                            │  │
│  │  {% block content %}                                      │  │
│  │    [Page-specific content here]                           │  │
│  │  {% endblock %}                                           │  │
│  │                                                            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ FOOTER                                                    │  │
│  │ © 2025 PCAP Analyzer | Version 1.0 | FastAPI + Tailwind  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

SCRIPTS:
- theme.js (dark mode toggle)
- {% block scripts %} (page-specific JS)
```

---

## Component Architecture

### Atomic Design Breakdown

```
ATOMS (Smallest units)
├── Buttons
│   ├── btn-primary (gradient background)
│   ├── btn-secondary (border outline)
│   ├── btn-danger (text danger color)
│   └── btn-icon (circular, icon only)
├── Badges
│   ├── badge-success (green gradient)
│   ├── badge-warning (orange solid)
│   ├── badge-danger (red solid)
│   └── badge-info (blue solid)
├── Icons
│   ├── SVG icons (Heroicons)
│   └── Emoji icons (📊, 📦, ✅, etc.)
├── Input fields
│   ├── text input (with focus ring)
│   ├── file input (hidden, triggered by zone)
│   └── select dropdown
└── Typography
    ├── Headings (H1-H3)
    ├── Body text (base, sm, xs)
    └── Mono text (code, task IDs)

MOLECULES (Combinations of atoms)
├── Card
│   ├── Header (title + optional icon)
│   ├── Body (content area)
│   └── Footer (actions)
├── Alert Box
│   ├── Icon (status indicator)
│   ├── Title (bold text)
│   └── Message (body text)
├── Progress Bar
│   ├── Container (gray background)
│   ├── Fill (gradient, width %)
│   └── Shimmer overlay (animation)
├── File Preview
│   ├── Icon (file type)
│   ├── Metadata (name, size)
│   └── Remove button
└── Status Badge + Text
    ├── Badge (colored pill)
    └── Status text

ORGANISMS (Complex components)
├── Upload Zone
│   ├── Drop area (border-dashed)
│   ├── Icon (cloud upload)
│   ├── Instructions (text)
│   ├── Specs badges (format, size)
│   └── Hidden file input
├── Progress Dashboard
│   ├── Overall progress bar
│   ├── Phase cards (grid 2 cols)
│   ├── Memory gauge
│   └── Live log panel
├── Analysis Card (History)
│   ├── File icon + name
│   ├── Metadata grid (status, health, date, size)
│   └── Action buttons (view, download, delete)
└── Header Navigation
    ├── Logo + brand
    ├── Nav links
    └── Theme toggle

TEMPLATES (Full pages)
├── upload.html (Landing)
├── progress.html (Analysis)
├── report.html (Results)
└── history.html (Archives)
```

---

## CSS Architecture

### Tailwind Layers Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                      TAILWIND CSS LAYERS                        │
└─────────────────────────────────────────────────────────────────┘

INPUT FILE: styles.css
│
├── @tailwind base;
│   │
│   ├── CSS Reset (normalize)
│   ├── Default HTML element styles
│   └── Custom base styles (if any)
│
├── @tailwind components;
│   │
│   ├── @layer components {
│   │   ├── .btn-primary (gradient button)
│   │   ├── .btn-secondary (outline button)
│   │   ├── .btn-danger (danger button)
│   │   ├── .card (white container)
│   │   ├── .card-gradient (colored bg)
│   │   ├── .badge-success (green pill)
│   │   ├── .badge-warning (orange pill)
│   │   ├── .badge-danger (red pill)
│   │   ├── .badge-info (blue pill)
│   │   ├── .alert-info (info box)
│   │   ├── .alert-warning (warning box)
│   │   ├── .alert-danger (error box)
│   │   └── .alert-success (success box)
│   │   }
│   │
│   └── 3rd party component styles
│
├── @tailwind utilities;
│   │
│   ├── All Tailwind utility classes
│   │   (spacing, colors, layout, etc.)
│   │
│   └── @layer utilities {
│       ├── .sr-only (screen reader only)
│       ├── .focus-visible (custom focus ring)
│       ├── .transition-theme (smooth color transition)
│       └── .shimmer-overlay (progress animation)
│       }
│
└── Custom CSS (outside @layer)
    ├── * { transition-theme } (dark mode smoothing)
    ├── .no-theme-transition (opt-out)
    └── Keyframes (@keyframes shimmer)

OUTPUT FILE: output.css (minified, purged in production)
│
└── Only classes used in HTML templates
    (Tailwind analyzes content[] paths and removes unused)
```

---

## JavaScript Architecture

### Script Organization

```
┌─────────────────────────────────────────────────────────────────┐
│                   JAVASCRIPT MODULES                            │
└─────────────────────────────────────────────────────────────────┘

/static/js/
│
├── theme.js (Loaded on all pages)
│   ├── IIFE (Immediately Invoked Function Expression)
│   ├── Detect system/localStorage preference
│   ├── Apply dark class to <html>
│   ├── Toggle on button click
│   └── Listen to system changes
│
├── upload.js (Loaded on upload.html)
│   ├── Drag & drop handlers
│   │   ├── dragover (show active state)
│   │   ├── dragleave (remove active state)
│   │   └── drop (handle file)
│   ├── File validation
│   │   ├── Check extension (.pcap, .pcapng)
│   │   ├── Check size (<= 500MB)
│   │   └── Show preview or error
│   ├── API: POST /api/upload
│   │   ├── FormData with file
│   │   ├── Fetch with progress
│   │   └── Redirect to /progress/{task_id}
│   └── Load recent analyses (API: GET /api/analyses?limit=3)
│
├── progress.js (Loaded on progress.html)
│   ├── SSE Connection
│   │   ├── EventSource(/api/progress/{task_id})
│   │   ├── onopen → log connected
│   │   ├── onmessage → handleProgressUpdate()
│   │   └── onerror → reconnect logic (max 5 attempts)
│   ├── Update UI Elements
│   │   ├── Overall progress bar (width %)
│   │   ├── Phase cards (status, tasks)
│   │   ├── Memory gauge (current/peak)
│   │   └── Live log (append entries)
│   ├── Handle Status Changes
│   │   ├── status: 'completed' → redirect to /report
│   │   └── status: 'failed' → show error alert
│   └── Accessibility
│       └── Update screen reader live region
│
├── report.js (Loaded on report.html)
│   └── Copy link to clipboard
│       ├── navigator.clipboard.writeText()
│       └── Show success feedback
│
└── history.js (Loaded on history.html)
    ├── Load analyses list (API: GET /api/analyses)
    ├── Search/Filter logic
    │   ├── Filter by status (dropdown)
    │   ├── Search by filename/task_id (input)
    │   └── Sort by date/size (dropdown)
    ├── Render analysis cards
    │   ├── Map over API response
    │   └── Generate HTML with badges
    └── Load statistics (API: GET /api/stats)
```

---

## Data Flow Architecture

### API Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                      API ENDPOINTS                              │
└─────────────────────────────────────────────────────────────────┘

FRONTEND                           BACKEND (FastAPI)
│
│ POST /api/upload
│ ────────────────────────────────► Validate file
│ Body: FormData(pcap_file)        │ - Check extension
│                                   │ - Check size
│                                   │ - Check magic bytes
│                                   │ Save to /data/uploads/{task_id}.pcap
│                                   │ Insert SQLite record
│                                   │ Enqueue task
│ {task_id, status}◄───────────────┘
│
│
│ GET /api/progress/{task_id}
│ ────────────────────────────────► Setup SSE stream
│ Headers: Accept: text/event-stream
│                                   │ Background worker analyzing
│                                   │ Callback sends SSE events:
│                                   │
│ data: {progress, phase, ...}◄─────┤ - Phase 1 updates
│ data: {progress, phase, ...}◄─────┤ - Phase 2 updates
│ data: {status: completed}◄────────┤ - Completion
│                                   │
│ [Connection closed after complete]│
│
│
│ GET /api/report/{task_id}
│ ────────────────────────────────► Check task status
│                                   │ If completed:
│                                   │   Return metadata (filename, date, etc.)
│                                   │ Else:
│ {metadata} or {error}◄────────────┤   Return error
│
│
│ GET /reports/{task_id}.html
│ ────────────────────────────────► Serve static HTML file
│                                   │ From /data/reports/{task_id}.html
│ HTML content◄─────────────────────┘
│
│
│ GET /download/{task_id}/{format}
│ ────────────────────────────────► Check file exists
│ format: 'html' or 'json'          │ Set Content-Disposition header
│                                   │ Stream file
│ File download◄────────────────────┘
│
│
│ GET /api/analyses
│ ────────────────────────────────► Query SQLite
│ Query params:                     │ SELECT * FROM analyses
│  - limit (default: 10)            │ WHERE created_at > NOW - 24h
│  - status (optional)              │ ORDER BY created_at DESC
│  - search (optional)              │ LIMIT ?
│                                   │
│ [{task_id, filename, ...}, ...]◄──┘
│
│
│ GET /api/stats
│ ────────────────────────────────► Aggregate SQLite
│                                   │ COUNT(*) GROUP BY status
│                                   │ SUM(file_size)
│ {total, completed, failed, ...}◄──┘
│
│
│ DELETE /api/analysis/{task_id}
│ ────────────────────────────────► Delete files
│                                   │ - /data/reports/{task_id}.*
│                                   │ UPDATE SQLite status='deleted'
│ {success: true}◄──────────────────┘
```

---

## State Management

### Page States Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    UPLOAD PAGE STATES                           │
└─────────────────────────────────────────────────────────────────┘

     ┌─────────────┐
     │   INITIAL   │  Drop zone visible, no file selected
     └──────┬──────┘
            │
            │ User selects/drops file
            │
            ▼
     ┌─────────────┐
     │ VALIDATING  │  Checking extension, size, magic bytes
     └──────┬──────┘
            │
       ┌────┴────┐
       │         │
   [VALID]   [INVALID]
       │         │
       ▼         ▼
  ┌────────┐  ┌────────┐
  │ READY  │  │ ERROR  │  Show error message, disable button
  └───┬────┘  └────────┘
      │
      │ User clicks "Analyze"
      │
      ▼
  ┌──────────┐
  │UPLOADING │  Show spinner, disable button
  └─────┬────┘
        │
   ┌────┴─────┐
   │          │
[SUCCESS]  [FAILED]
   │          │
   ▼          ▼
REDIRECT   ERROR STATE


┌─────────────────────────────────────────────────────────────────┐
│                  PROGRESS PAGE STATES                           │
└─────────────────────────────────────────────────────────────────┘

     ┌──────────────┐
     │ CONNECTING   │  Establishing SSE connection
     └──────┬───────┘
            │
            ▼
     ┌──────────────┐
     │  CONNECTED   │  Waiting for first update
     └──────┬───────┘
            │
            ▼
     ┌──────────────┐
     │  PHASE 1     │  Metadata extraction (dpkt)
     │  IN PROGRESS │  Progress: 0-50%
     └──────┬───────┘
            │
            ▼
     ┌──────────────┐
     │  PHASE 2     │  Deep analysis (Scapy)
     │  IN PROGRESS │  Progress: 50-100%
     └──────┬───────┘
            │
       ┌────┴────┐
       │         │
  [SUCCESS]  [FAILED]
       │         │
       ▼         ▼
  ┌─────────┐ ┌────────┐
  │COMPLETED│ │ ERROR  │  Show error details
  └────┬────┘ └────────┘
       │
       │ Auto-redirect (3s)
       │
       ▼
     REPORT PAGE


┌─────────────────────────────────────────────────────────────────┐
│                  HISTORY PAGE STATES                            │
└─────────────────────────────────────────────────────────────────┘

     ┌──────────────┐
     │   LOADING    │  Fetching analyses from API
     └──────┬───────┘
            │
       ┌────┴────┐
       │         │
  [HAS DATA] [EMPTY]
       │         │
       ▼         ▼
  ┌─────────┐ ┌────────┐
  │ LOADED  │ │ EMPTY  │  Show "No analyses yet" message
  │ (list)  │ │ STATE  │
  └────┬────┘ └────────┘
       │
       │ User types search/changes filter
       │
       ▼
  ┌──────────────┐
  │  FILTERED    │  Show filtered results
  └──────┬───────┘
         │
    ┌────┴────┐
    │         │
[HAS RESULTS][EMPTY]
    │         │
    ▼         ▼
 DISPLAY   NO RESULTS
  LIST      MESSAGE
```

---

## Responsive Breakpoints

### Layout Transformations

```
┌─────────────────────────────────────────────────────────────────┐
│              MOBILE (< 640px) "xs"                              │
└─────────────────────────────────────────────────────────────────┘

Upload Zone:
┌────────────────┐
│  Stack         │  - Icon: w-16 h-16
│  vertically    │  - Padding: p-8
│  Single column │  - Text: text-base
└────────────────┘

Progress Dashboard:
┌────────────────┐
│ Phase 1        │
│ (Full width)   │
├────────────────┤
│ Phase 2        │
│ (Full width)   │
├────────────────┤
│ Memory Gauge   │
│ (Full width)   │
├────────────────┤
│ Live Log       │
│ (Full width)   │
└────────────────┘

History Cards:
┌────────────────┐
│ Analysis 1     │  - All metadata stacked
│ Metadata       │  - Buttons stacked
│ [View] [DL]    │
├────────────────┤
│ Analysis 2     │
│ ...            │
└────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│              TABLET (640px - 1024px) "sm/md"                    │
└─────────────────────────────────────────────────────────────────┘

Upload Zone:
┌────────────────────────────┐
│  Icon: w-18 h-18           │  - Padding: p-10
│  Text: text-lg             │  - 2 badges inline
│  [badge1] [badge2]         │
└────────────────────────────┘

Progress Dashboard:
┌──────────────┬──────────────┐
│ Phase 1      │ Phase 2      │  - 2 columns grid
│ (50% width)  │ (50% width)  │
├──────────────┴──────────────┤
│ Memory Gauge (Full width)   │
├─────────────────────────────┤
│ Live Log (Full width)       │
└─────────────────────────────┘

History Cards:
┌──────────────────────────────┐
│ Icon  Filename               │  - Metadata 2 cols
│ Date  Size                   │  - Buttons inline
│ [View] [Download] [Delete]   │
└──────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│              DESKTOP (> 1024px) "lg/xl/2xl"                     │
└─────────────────────────────────────────────────────────────────┘

Upload Zone:
┌──────────────────────────────────────┐
│  Icon: w-20 h-20                     │  - Padding: p-12
│  Text: text-xl                       │  - Max-width container
│  Centered, spacious                  │  - Hover effects prominent
└──────────────────────────────────────┘

Progress Dashboard:
┌──────────────┬──────────────┐
│ Phase 1      │ Phase 2      │  - 2 columns grid
│ (50% width)  │ (50% width)  │  - Sub-tasks visible
│ All tasks    │ All tasks    │
├──────────────┴──────────────┤
│ Memory Gauge (Full width)   │  - Larger bars
├──────────────┬──────────────┤
│ Live Log     │ Statistics   │  - Optional 2nd col
│ (60% width)  │ (40% width)  │
└──────────────┴──────────────┘

History Cards:
┌────────────────────────────────────────────────────┐
│ 📦 Filename    Status    Health   Date   Size      │  - Grid layout
│ Task ID        [Badge]   95/100   15:30  26.4 MB   │  - All inline
│                                                     │
│                      [View] [Download] [Delete]    │  - Right-aligned
└────────────────────────────────────────────────────┘

Features Grid (Landing):
┌──────────────┬──────────────┬──────────────┐
│ Feature 1    │ Feature 2    │ Feature 3    │  - 3 columns
│ ⚡ Fast      │ 📊 Detailed  │ 🛡️ Secure   │
└──────────────┴──────────────┴──────────────┘
```

---

## Color System Hierarchy

### Light Mode Palette

```
┌─────────────────────────────────────────────────────────────────┐
│                     LIGHT MODE COLORS                           │
└─────────────────────────────────────────────────────────────────┘

PRIMARY ACTIONS                 BACKGROUNDS
┌──────────────┐               ┌──────────────┐
│  #3498db     │ Primary       │  #f5f5f5     │ Body
│  Blue        │               │  Very Light  │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #2c3e50     │ Secondary     │  #ffffff     │ Container
│  Dark Blue   │               │  White       │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #1abc9c     │ Accent        │  #f8f9fa     │ Section
│  Teal        │               │  Light Gray  │
└──────────────┘               └──────────────┘

SEMANTIC COLORS                 BORDERS
┌──────────────┐               ┌──────────────┐
│  #27ae60     │ Success       │  #ecf0f1     │ Light
│  Green       │               │  Gray        │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #f39c12     │ Warning       │  #2c3e50     │ Primary
│  Orange      │               │  Dark        │
└──────────────┘               └──────────────┘
┌──────────────┐
│  #e74c3c     │ Danger        SHADOWS
│  Red         │               ┌──────────────┐
└──────────────┘               │ rgba(0,0,0   │
┌──────────────┐               │ ,0.1-0.3)    │
│  #3498db     │ Info          │ Black alpha  │
│  Blue        │               └──────────────┘
└──────────────┘

GRADIENTS (Signature visuals)
┌────────────────────────────────────┐
│ Primary:  #667eea → #764ba2        │ Purple gradient
└────────────────────────────────────┘
┌────────────────────────────────────┐
│ Success:  #11998e → #38ef7d        │ Green gradient
└────────────────────────────────────┘
┌────────────────────────────────────┐
│ Warning:  #f093fb → #f5576c        │ Pink gradient
└────────────────────────────────────┘
┌────────────────────────────────────┐
│ Danger:   #fa709a → #fee140        │ Red-yellow gradient
└────────────────────────────────────┘
```

### Dark Mode Palette

```
┌─────────────────────────────────────────────────────────────────┐
│                     DARK MODE COLORS                            │
└─────────────────────────────────────────────────────────────────┘

BACKGROUNDS                     TEXTS
┌──────────────┐               ┌──────────────┐
│  #1a1a1a     │ Body          │  #e0e0e0     │ Primary
│  Black       │               │  Light Gray  │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #2a2a2a     │ Container     │  #90a4ae     │ Secondary
│  Dark Gray   │               │  Blue Gray   │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #333333     │ Section       │  #4fc3f7     │ Accent
│  Gray        │               │  Light Blue  │
└──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐
│  #1e1e1e     │ Code Block    │  #8ab4f8     │ Code
│  Dark        │               │  Pastel Blue │
└──────────────┘               └──────────────┘

BORDERS                         SEMANTIC (unchanged)
┌──────────────┐               ┌──────────────┐
│  #404040     │ Primary       │  #27ae60     │ Success
│  Medium Gray │               │  Green       │
└──────────────┘               └──────────────┘
                               ┌──────────────┐
SHADOWS                        │  #f39c12     │ Warning
┌──────────────┐               │  Orange      │
│ rgba(0,0,0   │               └──────────────┘
│ ,0.3-0.5)    │               ┌──────────────┐
│ Darker alpha │               │  #e74c3c     │ Danger
└──────────────┘               │  Red         │
                               └──────────────┘

GRADIENTS (unchanged)
Same as light mode - colors already work well in dark
```

---

## Animation Timing

### Transition Speeds

```
┌─────────────────────────────────────────────────────────────────┐
│                   ANIMATION TIMING GUIDE                        │
└─────────────────────────────────────────────────────────────────┘

INSTANT (0ms)
├── Theme toggle (dark mode switch)
└── Click feedback (button active state)

FAST (100-200ms)
├── Hover states (buttons, cards, links)
├── Focus rings (keyboard navigation)
└── Dark mode color transitions

NORMAL (200-300ms)
├── Page transitions (fade in/out)
├── Modal open/close
├── Alert appearance
└── Dropdown menus

SLOW (300-500ms)
├── Progress bar updates (width change)
├── Card hover lift (translateY)
└── Accordion expand/collapse

VERY SLOW (500ms+)
├── SSE log entry fade in (500ms)
├── Shimmer effect loop (2s infinite)
└── Auto-redirect countdown (3s)

EASING FUNCTIONS
├── ease-out    → Entries (fade in, slide up)
├── ease-in     → Exits (fade out, slide down)
├── ease-in-out → Toggles (expand/collapse)
└── linear      → Infinite loops (shimmer, spinner)
```

---

## Accessibility Tree

### ARIA Roles & Labels

```
┌─────────────────────────────────────────────────────────────────┐
│                   ACCESSIBILITY STRUCTURE                       │
└─────────────────────────────────────────────────────────────────┘

<html lang="fr">
│
├── <a href="#main-content" class="sr-only focus:not-sr-only">
│   └── [Skip to main content]  ← Keyboard users
│
├── <header>
│   ├── <nav aria-label="Main navigation">
│   │   ├── <a href="/">Upload</a>
│   │   └── <a href="/history">History</a>
│   │
│   └── <button id="theme-toggle" aria-label="Toggle dark mode">
│       └── [Sun/Moon icon]
│
├── <main id="main-content">
│   │
│   ├── UPLOAD PAGE
│   │   ├── <div role="button" aria-label="Upload PCAP file...">
│   │   │   └── <input type="file" aria-label="PCAP file input">
│   │   │
│   │   └── <button aria-label="Analyze PCAP file">
│   │
│   ├── PROGRESS PAGE
│   │   ├── <div role="progressbar"
│   │   │        aria-valuenow="65"
│   │   │        aria-valuemin="0"
│   │   │        aria-valuemax="100"
│   │   │        aria-label="Analysis progress: 65%">
│   │   │
│   │   ├── <div aria-live="polite" aria-atomic="true">
│   │   │   └── [SSE updates for screen readers]
│   │   │
│   │   └── <div role="log" aria-label="Analysis log">
│   │       └── [Live log entries]
│   │
│   ├── REPORT PAGE
│   │   ├── <iframe title="PCAP Analysis Report"
│   │   │           sandbox="allow-same-origin allow-scripts">
│   │   │
│   │   └── <button aria-label="Copy report link">
│   │
│   └── HISTORY PAGE
│       ├── <label for="search">
│       │   └── <input id="search" aria-label="Search analyses">
│       │
│       ├── <label for="status-filter">
│       │   └── <select id="status-filter">
│       │
│       └── <div role="status" aria-live="polite">
│           └── [Filter result count]
│
└── <footer>
    └── [Copyright info]

LANDMARK ROLES (implicit)
├── header  → <header>
├── nav     → <nav>
├── main    → <main>
└── contentinfo → <footer>
```

---

## Performance Budget

### Target Metrics

```
┌─────────────────────────────────────────────────────────────────┐
│                   PERFORMANCE TARGETS                           │
└─────────────────────────────────────────────────────────────────┘

LIGHTHOUSE SCORES (Goal: >90)
┌─────────────────────┬──────────┬──────────┐
│ Category            │ Target   │ Weight   │
├─────────────────────┼──────────┼──────────┤
│ Performance         │ 90-100   │ Critical │
│ Accessibility       │ 95-100   │ Critical │
│ Best Practices      │ 90-100   │ High     │
│ SEO                 │ 90-100   │ Medium   │
└─────────────────────┴──────────┴──────────┘

CORE WEB VITALS
┌──────────────────────┬──────────┬──────────┐
│ Metric               │ Good     │ Poor     │
├──────────────────────┼──────────┼──────────┤
│ LCP (Largest Paint)  │ <2.5s    │ >4.0s    │
│ FID (First Input)    │ <100ms   │ >300ms   │
│ CLS (Layout Shift)   │ <0.1     │ >0.25    │
└──────────────────────┴──────────┴──────────┘

LOAD TIMES (3G connection)
┌───────────────────────┬──────────┐
│ Page                  │ Target   │
├───────────────────────┼──────────┤
│ Upload (first load)   │ <3s      │
│ Upload (cached)       │ <1s      │
│ Progress              │ <2s      │
│ Report (iframe)       │ <4s      │
│ History               │ <2.5s    │
└───────────────────────┴──────────┘

ASSET SIZES
┌───────────────────────┬──────────┐
│ Asset                 │ Max Size │
├───────────────────────┼──────────┤
│ output.css (minified) │ <50KB    │
│ JavaScript total      │ <100KB   │
│ Images (total)        │ <200KB   │
│ Fonts                 │ 0KB      │
│                       │ (system) │
└───────────────────────┴──────────┘

OPTIMIZATION TECHNIQUES
├── Tailwind CSS purge (removes unused classes)
├── CSS minification (production build)
├── JS minification (terser)
├── Gzip compression (server-side)
├── HTTP/2 multiplexing (FastAPI)
├── Cache headers (static assets)
└── Lazy loading (images, iframes)
```

---

## Security Considerations

### Design Security Features

```
┌─────────────────────────────────────────────────────────────────┐
│                   SECURITY BY DESIGN                            │
└─────────────────────────────────────────────────────────────────┘

CLIENT-SIDE VALIDATION
├── File extension check (.pcap, .pcapng only)
├── File size check (max 500MB)
├── Visual feedback on validation errors
└── Disable submit if validation fails

CSP HEADERS (Content Security Policy)
default-src 'self';
script-src 'self' 'unsafe-inline';  ← Tailwind requires inline
style-src 'self' 'unsafe-inline';   ← Inline critical CSS
img-src 'self' data:;               ← Data URIs for icons

IFRAME SANDBOX (Report page)
sandbox="allow-same-origin allow-scripts"
├── Prevents form submission from iframe
├── Prevents top-level navigation
└── Isolates embedded report

NO EXTERNAL DEPENDENCIES
├── No CDN resources (except dev)
├── All assets served from same-origin
├── No third-party analytics/tracking
└── System fonts only (no web fonts)

XSS PREVENTION
├── Jinja2 auto-escaping ({{ var }})
├── No innerHTML usage (use textContent)
├── Sanitize API responses before rendering
└── ARIA labels use static text only

CSRF PROTECTION (Backend)
├── SameSite cookies (if session-based)
├── CORS headers configured
└── Origin validation

USER DATA PRIVACY
├── Files deleted after 24h (automatic)
├── No persistent user accounts
├── No tracking or analytics
└── Local storage only for theme preference
```

---

## Conclusion

Cette architecture design couvre tous les aspects de l'interface web du PCAP Analyzer, de la structure des pages à la gestion des états, en passant par la performance et la sécurité.

**Points clés:**
- Flow utilisateur clair et intuitif
- Architecture composants modulaire (Atomic Design)
- Responsive design mobile-first
- Performance optimisée (budget <50KB CSS)
- Accessibilité WCAG 2.1 AA complète
- Sécurité intégrée dès le design

**Prochaines étapes:**
1. Valider l'architecture avec l'équipe
2. Prototyper les composants critiques
3. Tester les flows utilisateur
4. Implémenter progressivement

---

**Créé par:** Agent UX/UI Designer
**Date:** 2025-12-12
**Version:** 1.0
