# Web framework dependencies (additional to requirements.txt)
# These are required for the web interface on top of the CLI tool

# FastAPI + ASGI server
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0
httpx>=0.25.0  # Required by TestClient for testing
email-validator>=2.0.0  # EmailStr validation in Pydantic

# File handling
python-multipart>=0.0.6  # Upload multipart/form-data
aiofiles>=23.2.1         # Async file operations

# Scheduling
apscheduler>=3.10.0,<4.0  # Cleanup scheduler

# Database (SQLite async + PostgreSQL)
aiosqlite>=0.19.0,<1.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT token generation/validation
passlib[bcrypt]>=1.7.4            # Password hashing with bcrypt
fastapi-csrf-protect>=0.3.4,<1.0  # CSRF protection (OWASP ASVS 4.2.2)

# Optional: Monitoring (uncomment if needed)
# prometheus-client>=0.19.0  # Prometheus metrics
